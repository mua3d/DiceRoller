import 'package:flutter/material.dart';

class Dice extends StatelessWidget {
  const Dice(this.image, {super.key});

  final Image image;

  @override
  Widget build(BuildContext context) {
    return image;
  }
}